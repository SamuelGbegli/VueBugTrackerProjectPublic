//Determines the order projects are sorted.
const SortOrder = {
  // Sorts projects by their sort condition from lowest to highest
  Ascending: 0,
  // Sorts projects by their sort condition from higwest to lowest
  Descending: 1
}

export default SortOrder;
